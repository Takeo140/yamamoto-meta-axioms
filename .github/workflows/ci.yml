- name: Verify Logic and Output 42
run: |
# Run the script and capture the output
OUTPUT=$(python verify_42.py)
echo "$OUTPUT"
# Check if "Result: 42" exists in the output
          echo "$OUTPUT" | grep "Result: 42"
